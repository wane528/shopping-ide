---
// src/components/home/CategoryGrid.astro
const categories = [
  { 
    name: 'Urinary Health', 
    slug: 'urinary-health', 
    description: 'FLUTD, UTIs, crystals, and bladder care guides',
    emoji: '🩺'
  },
  { 
    name: 'Kidney Disease', 
    slug: 'kidney-disease', 
    description: 'CKD stages, treatment, diet, and home care',
    emoji: '🫘'
  },
  { 
    name: 'Product Reviews', 
    slug: 'reviews', 
    description: 'Prescription diets, supplements, and fountains',
    emoji: '⭐'
  },
  { 
    name: 'Care Tips', 
    slug: 'care-tips', 
    description: 'Hydration, nutrition, and daily wellness tips',
    emoji: '💡'
  },
];
---

<section class="section bg-gray-50 dark:bg-gray-950">
  <div class="container-content">
    <div class="text-center mb-12">
      <h2 class="text-h1 text-gray-900 dark:text-white mb-3">
        Explore by Topic
      </h2>
      <p class="text-body-l text-gray-500 dark:text-gray-400 max-w-xl mx-auto">
        Find the information you need to keep your cat healthy and comfortable.
      </p>
    </div>
    
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {categories.map((cat) => (
        <a 
          href={`/category/${cat.slug}`} 
          class="group card card-hover text-center"
        >
          <div class="text-3xl mb-3">{cat.emoji}</div>
          <h3 class="font-semibold text-gray-900 dark:text-white mb-2">
            {cat.name}
          </h3>
          <p class="text-body-s text-gray-500 dark:text-gray-400">
            {cat.description}
          </p>
        </a>
      ))}
    </div>
  </div>
</section>
